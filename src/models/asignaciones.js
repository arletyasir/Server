class Asignacion {
    constructor(order_id, usuario_id, fecha_asignacion) {
      this.order_id = order_id;
      this.usuario_id = usuario_id;
      this.fecha_asignacion = fecha_asignacion;
    }
  }
  
  export default Asignacion;
  